import { changeIsDone, initialStateType, usersReduser } from "./users-reducer"



test('change followed', () => {
    // let initialState: initialStateType = {
    //     users: [
    //         { name: 'Ivan', followed: true, status: 'Hello frends', id: 1, photos: { small: null, large: null }},
    //         { name: 'Alex', followed: false, status: 'Ok', id: 2, photos: { small: null, large: null }},
    //         { name: 'Vasy', followed: true, status: 'op', id: 3, photos: { small: null, large: null }}
    //     ]
    // }

    // let endState = usersReduser(initialState, changeIsDoneAC(false, 1))
    // expect(endState.users[0].followed).toBe(false)
    // expect(endState.users[1].followed).toBe(false)
})